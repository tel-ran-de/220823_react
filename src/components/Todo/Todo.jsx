export const Todo = ({ taskTitle, taskDescription }) => {
  return (
    <div style={{}}>
      <h1>{taskTitle}</h1>
      <p>{taskDescription}</p>
    </div>
  )
}

// пропсы соответстовать названиям в объекте
// key
